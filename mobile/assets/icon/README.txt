Drop your app icon here as:

  app_icon.png

Requirements:
  - Square PNG (1024 x 1024 recommended)
  - Logo centered with a little padding (corners get cropped on some phones)

Then from the mobile folder run:

  flutter pub get
  dart run flutter_launcher_icons

Uninstall the app from your phone/emulator, then flutter run again (icons cache easily).
